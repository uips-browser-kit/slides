# talks

One subfolder per conference or community talk.

Naming convention: `YYYY-<event-slug>/`

Each subfolder must contain:
- `index.html` — the reveal.js deck
- `README.md` — title, event name, date, abstract (one paragraph)
