# assets

Shared images, logos, fonts, and CSS theme overrides used across decks.

```
assets/
  img/        ← PNG/SVG logos and diagrams
  theme/      ← custom reveal.js CSS theme (import from individual decks)
```

Do not copy assets into individual deck folders — reference them with a
relative path (`../../assets/img/logo.svg`).
